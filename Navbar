import React from 'react';
import Chart from 'chart.js/auto';
import 'chartjs-chart-financial'; // Import financial plugin

const CandlestickChart = ({ candlestickData }) => {
  const canvasRef = React.useRef();

  React.useEffect(() => {
    if (!candlestickData || candlestickData.length === 0) return;

    const ctx = canvasRef.current.getContext('2d');
    new Chart(ctx, {
      type: 'candlestick', // Use 'candlestick' type
      data: {
        datasets: [{
          data: candlestickData,
          label: 'Candlestick Data'
        }]
      },
      options: {
        // Customize options
      }
    });
  }, [candlestickData]);

  return <canvas ref={canvasRef}></canvas>;
};

// RSIChart component remains the same

const ChartComponent = () => {
  // Static candlestick data
  const candlestickData = [
    { t: 1, o: 100, h: 120, l: 90, c: 110 },
    { t: 2, o: 110, h: 130, l: 95, c: 115 },
    { t: 3, o: 115, h: 135, l: 100, c: 125 },
  ];

  // Static RSI data
  const rsiData = [
    { x: 1, y: 70 },
    { x: 2, y: 75 },
    { x: 3, y: 80 },
  ];

  return (
    <div>
      <h1>Candlestick Chart</h1>
      <CandlestickChart candlestickData={candlestickData} />
      <h1>RSI Chart</h1>
      {/* Include RSIChart component here */}
    </div>
  );
};

export default ChartComponent;
